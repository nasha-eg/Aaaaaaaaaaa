
# 🌟 شركة العاصمة للفحم - النظام الديناميكي الشامل
**Al-Asimh Charcoal - Full Dynamic CMS Application**

تطبيق ويب متكامل ومبني باستخدام **React.js**، مخصص لشركة تصدير فحم بمعايير عالمية. يشتمل النظام على واجهة مستخدم مزدوجة اللغة (عربي/إنجليزي)، إضافة إلى **لوحة تحكم (CMS) مدمجة** تتيح التعديل الحي على كافة نصوص، صور، وإعدادات الموقع دون الحاجة للمس الكود البرمجي.

---

## ✨ المميزات الرئيسية (Features)
1. **بدون أدوات بناء معقدة (No Build Step):** يعمل المشروع مباشرة عبر المتصفح باستخدام تقنية `Import Maps` و `ES Modules` (ESM)، مما يعني أنك لست بحاجة إلى تشغيل `npm install` أو `npm run build`.
2. **لوحة إدارة ديناميكية (Dynamic Admin Dashboard):** تحكم كامل في (أيقونة الموقع، صورة الغلاف، الكتالوج، معرض الصور، آراء العملاء، والسيو).
3. **متعدد اللغات (Multi-language):** دعم كامل للغتين العربية والإنجليزية بضغطة زر وتغيير اتجاه العرض (RTL / LTR).
4. **مساعد ذكي (AI Chat Widget):** مدمج بتقنية **Google Gemini API** للرد الآلي على استفسارات العملاء.
5. **تهيئة قواعد البيانات (Database Ready):** واجهة مخصصة لربط الموقع بقاعدة بيانات `MySQL`.

---

## 🚀 طريقة التشغيل والتركيب (Installation & Setup)

بما أن المشروع يستخدم تقنية الاستيراد المباشر، فإن تشغيله يعتبر الأسهل على الإطلاق:

### الطريقة الأولى: باستخدام (VS Code Live Server) - *موصى بها*
1. قم بفتح مجلد المشروع باستخدام محرر الأكواد `Visual Studio Code`.
2. تأكد من تثبيت إضافة **Live Server**.
3. اضغط بزر الفأرة الأيمن على ملف `index.html` واختر **Open with Live Server**.
4. سيعمل الموقع مباشرة على المتصفح (غالباً على الرابط `http://127.0.0.1:5500`).

### الطريقة الثانية: باستخدام (Python)
إذا كان لديك بايثون مثبت على جهازك، افتح موجه الأوامر (Terminal) داخل مجلد المشروع واكتب:
```bash
python -m http.server 8000
```
ثم افتح المتصفح على الرابط `http://localhost:8000`.

---

## 🔐 الدخول إلى لوحة التحكم (Admin Dashboard)

الواجهة الأمامية للموقع تحتوي على بوابة سرية للوصول إلى لوحة الإدارة الشاملة:

1. انزل إلى **أسفل الموقع تماماً** (فوق الفوتر).
2. ستجد نصاً شفافاً صغيراً جداً مكتوب عليه: **`SYSTEM DASHBOARD LOGIN`**.
3. اضغط عليه لتفتح لك شاشة تسجيل الدخول الآمنة.
4. استخدم البيانات التالية للدخول:
   - **اسم المستخدم:** `admin`
   - **كلمة المرور:** `1997`

*(ملاحظة: يمكنك تغيير كلمة المرور من داخل ملف `components/AdminDashboard.tsx` سطر `43`)*.

---

## 🗄️ ربط قاعدة البيانات (MySQL Database Integration)

الموقع مهيأ حالياً ليكون **Frontend** قوي جداً يحتفظ بالبيانات في حالة التطبيق (State). لربط لوحة التحكم وجعل التعديلات دائمة (Persistent) في قاعدة بيانات حقيقية:

1. ادخل إلى **لوحة الإدارة** -> تبويب **🗄️ ربط قاعدة البيانات**.
2. ستجد حقول (Host, Port, Username, Password, Database Name).
3. **هيكلة الـ Backend المطلوبة:**
   - تحتاج إلى إنشاء خادم بسيط (مثلاً باستخدام `Node.js / Express` أو `PHP`).
   - يقوم الخادم باستقبال طلبات `POST` من الواجهة (لوحة الإدارة) ليقوم بتخزين البيانات (الإعدادات، المنتجات، الصور) في جداول MySQL.
   - عند تحميل الموقع (في ملف `App.tsx` داخل `useEffect`)، يجب عمل طلب `GET` (Fetch API) للخادم الخاص بك لاستدعاء الإعدادات من `MySQL` ووضعها كقيمة مبدئية بدلاً من `initialSettings`.

---

## 🤖 إعداد المساعد الذكي (Gemini AI Chat)

المساعد الذكي يعتمد على مفتاح واجهة برمجة تطبيقات من جوجل.
1. تم استدعاء الخدمة في ملف `services/geminiService.ts`.
2. لكي يعمل الشات، يجب توفير `API_KEY` الخاص بـ Gemini.
3. في بيئات الإنتاج الحقيقية، يجب وضع المفتاح ضمن متغيرات البيئة `process.env.API_KEY` أو تمريره عبر الـ Backend لحمايته من الظهور للعامة.

---

## 🛠️ تقنيات المشروع (Tech Stack)
* **React 19** (عبر esm.sh)
* **Tailwind CSS** (عبر CDN) للتصميم المتجاوب.
* **Google Gemini API** للمساعد الذكي.
* **TypeScript** لهيكلة الواجهات وضمان خلو الكود من الأخطاء.

---
**تطوير وتصميم:** أشرف ماهر - شركة سوشيال براند (Social Brand Co.)
